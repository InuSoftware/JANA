{
  "name": "jana-comandas",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:slow": "vitest run --testNamePattern slow",
    "test:fast": "vitest run --exclude '**/performance.test.js'",
    "test:update-snapshots": "vitest run -u --testNamePattern snapshots"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "vitest": "^3.2.4"
  }
}
